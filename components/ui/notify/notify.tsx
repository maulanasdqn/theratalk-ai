import { FC, ReactElement } from "react";

export const Notify: FC = (): ReactElement => {
  return (
    <div className="flex w-full relative">
      <div className="absolute">Notify</div>
    </div>
  );
};
